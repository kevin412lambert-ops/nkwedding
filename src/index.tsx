import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import WeddingWebsite from './wedding-website.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WeddingWebsite />
  </React.StrictMode>
);
